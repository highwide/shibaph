# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html
DEFAULTS: &DEFAULTS
  practice: oss
  done_at: <%= Time.zone.today %>
  goal: 1
  progress: 1

yesterday_progress:
  practice: no_today_progress
  done_at: <%= Time.zone.yesterday %>
  goal: 5
  progress: 10

start_of_range_progresses: &START_RANGE
  <<: *DEFAULTS
  practice: for_range_progresses
  done_at: <%= Time.zone.today - 9.days %>
  progress: 1

longest_streak_progress1:
  <<: *START_RANGE
  done_at: <%= Time.zone.today - 7.days %>

longest_streak_progress2:
  <<: *START_RANGE
  done_at: <%= Time.zone.today - 6.days %>

longest_streak_progress3:
  <<: *START_RANGE
  done_at: <%= Time.zone.today - 5.days %>

not_done_progress:
  <<: *START_RANGE
  done_at: <%= Time.zone.today - 4.days %>
  progress: 0

current_streak_progress1:
  <<: *START_RANGE
  done_at: <%= Time.zone.yesterday %>

current_streak_progress2:
  <<: *START_RANGE
  done_at: <%= Time.zone.today %>
